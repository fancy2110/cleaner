use serde::de::DeserializeOwned;
use tauri::{plugin::PluginApi, AppHandle, Runtime};

use crate::models::*;

pub fn init<R: Runtime, C: DeserializeOwned>(
    app: &AppHandle<R>,
    _api: PluginApi<R, C>,
) -> crate::Result<Filemanager<R>> {
    Ok(Filemanager(app.clone()))
}

/// Access to the filemanager APIs.
pub struct Filemanager<R: Runtime>(AppHandle<R>);

impl<R: Runtime> Filemanager<R> {
    pub fn ping(&self, payload: PingRequest) -> crate::Result<PingResponse> {
        Ok(PingResponse {
            value: payload.value,
        })
    }
}
